#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int x1, x2, y1, y2, height, length, perimeter, square;
    cin >> x1 >> y1>>x2>>y2;
    
    heigth = y1 - y2;
    lenght = x1 - x2;
    heigth = abs(heigth);
    length = abs(length);
    
    perimeter = 2 * length + 2*heigth;
    square = heigth * length;
    
    cout<<"area: "<<square << " perimeter: "<<perimeter;
    
    return 0;
}
