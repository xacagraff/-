#include <iostream>
#include <math.h>
using namespace std;
int main()
{   
    int A, B, C, AC, BC;
    cin >> A >> B >> c;
    AC = C -A;
    BC = C - B;
    cout << abs(AC) + abs(BC);
    return 0;

}
