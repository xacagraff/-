#include <iostream>
#include <cmath>
using namespace std;
int main ()
{

    setlocale(0, "");
    float x, y;
    cout<<"Введите значение x = ";
    cin>>x;
    y = 3*pow(x,6) - 6*pow(x,2) - 7;
    cout<<"Значение функции y = 3*(x^6) - 6*(x^2) - 7 равен "<<y;
 

  return 0;

}
